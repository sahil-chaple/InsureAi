# ── Core ──
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0

# ── Auth (JWT + JWKS) ──
PyJWT[crypto]>=2.8,<3.0
# PyJWT[crypto] pulls in cryptography for RS256 support

# ── Database ──
asyncpg>=0.29,<1.0
sqlalchemy[asyncio]>=2.0,<3.0

# ── Redis ──
redis>=5.0,<6.0

# ── Utilities ──
python-dotenv>=1.0,<2.0
httpx>=0.27,<1.0
